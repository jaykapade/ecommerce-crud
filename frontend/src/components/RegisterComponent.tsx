import {
  Box,
  Grid,
  TextField,
  InputLabel,
  Typography,
  Button,
  Divider,
  CircularProgress,
} from "@mui/material";

const RegisterComponent = () => {
  return <Box>RegisterComponent</Box>;
};

export default RegisterComponent;
